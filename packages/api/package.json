{
  "name": "@kuruma/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kuruma/shared": "workspace:*",
    "hono": "^4"
  },
  "devDependencies": {
    "vitest": "^2",
    "wrangler": "^4.80.0"
  }
}
